/**
 * Re-export all types from the canonical SDK package.
 *
 * This file exists so that existing UI imports (`$lib/types.ts`) continue
 * working without modification after the monorepo restructuring.
 */
export * from '@tilt-launcher/sdk';
